---
title: {{title}} — Tasks
type: tasks
parent_plan: {{parent_plan}}
created: {{date}}
updated: {{date}}
---

# {{title}} — Tasks (sub-plan {{sub_plan_id}})

**Spec:** `{{plan_dir}}/spec.md`
**Last Updated:** {{date}}

## Status Key

`not-started` | `in-progress` | `done` | `blocked` | `cut`

<!--
SHAPE CONTRACT — governance/file-type-contracts/tasks.md.json. Structure = ledger-at-top +
per-task-at-bottom, and BOTH surfaces are RENDERER-OWNED: the tasks:start/end sentinel pair
bounds the librarian:tasks-render generated region, and tasks-render is the SINGLE writer of it
(/R-37). RULING 2 (): this scaffolder ships an EMPTY sentinel pair and DELEGATES
the region-fill to tasks-render at scaffold time (new-plan.sh render_sub_quartet -> delegate_render),
so a fresh sub-plan carries exactly ONE renderer-owned `## Tasks` INSIDE the sentinels and no
statically pre-emitted duplicate. NEVER hand-author a `## Tasks` heading or per-task block outside
the sentinels, and NEVER hand-edit inside them — task-state SoT is manifest.tasks[]. The literal
marker lines appear ONLY as the real pair below — never inside this comment (non-literal form only;
the line-anchored parser blocks on a stray marker line).
SUB-PLAN tasks: a sub-plan CARRIES real tasks (unlike the master, which aggregates status only and
is renderer-SKIPPED). The rendered ledger mirrors manifest.tasks[] (tasks-render --check parity).
Task IDs use T-N (T-1, T-2, T-3.5 …) and are IMMUTABLE once published. R-37 LOCKSTEP: ledger row
count == manifest.tasks[] length; status flips sync via the handoff.md `<!-- task-done: NN/T-M -->`
sentinels (). Per-task Description 200-800 tokens; File References are absolute paths.
parent_plan is REQUIRED in frontmatter at depth >= 3 (R-28).
-->

<!-- tasks:start -->

<!-- tasks:end -->
