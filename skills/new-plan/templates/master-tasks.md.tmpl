---
title: {{title}} — Tasks
type: tasks
created: {{date}}
updated: {{date}}
---

# {{title}} — Tasks (master)

**Spec:** `{{plan_dir}}/spec.md`
**Last Updated:** {{date}}

## Status Key

`not-started` | `in-progress` | `done` | `blocked` | `cut`

<!--
SHAPE CONTRACT — governance/file-type-contracts/tasks.md.json.
MASTER tasks.md: a master AGGREGATES sub-plan STATUS, not sub-plan TASKS (realign-2
). A master carries NO renderer-owned tasks region — librarian:tasks-render DETECTS a
master (type:master / sub_plans) and SKIPS it, so the sub-plan status rollup below is plain
HAND-OWNED markdown: maintain it by hand. There is no tasks:start/end sentinel pair on a master
(the never-hand-edit-inside-the-sentinels contract binds a per-plan tasks.md, NOT a master).
The master manifest.tasks[] MAY be empty; real per-task breakdown lives in each sub-plan's
tasks.md / manifest.tasks[]. R-37 LOCKSTEP: if the master needs coordinating tasks, keep this
rollup and the same-commit manifest.tasks[] update in step.
-->

## Sub-plan status rollup

| Sub-plan | Slug | Status |
|----------|------|--------|
| 01 | `01-{{first_sub_slug}}` | planned |

---

## Tasks

_A master aggregates sub-plan status; it carries no tasks of its own by default. Per-sub-plan task breakdown lives in `{{plan_dir}}/NN-<sub-slug>/tasks.md`. If the master needs coordinating tasks, add them here AND to `manifest.tasks[]` in the same commit (R-37)._
