{{provenance_frontmatter}}
title: {{candidate.label}} — Updates
type: updates
status: active
created: {{generated_at_date}}
audience: {{audience}}
tags:
{{tags_yaml_list}}
---

# {{candidate.label}} — Updates

> _Updates scaffolded from seeded content. The seeding pass populates
> the **Seeded** entry below with a one-line provenance pointer; new
> updates append to the top of the timeline as the project evolves.
> Each entry: date heading, then narrative + decisions + next steps._

## Timeline

### {{generated_at_date}} — Seeded from onboarding content

This project was scaffolded from {{source_items_count}} source items
during `/onboard --seed-content`. See `PRD.md` for the proposal,
`Context.md` for the source-item digest, and the
`seed-content-pipeline` audit log at
`onboarding/auto-author-log.jsonl` for the full provenance trail.

Source items grouped under this project at Stage 2 INFER:

{{source_items_bullet_list}}

_(Above bullet list is a provenance pointer, not the project history.
Real updates start below.)_

---

_Append new updates above this line. Newest first._
