---
type: index
tags: ["#project/<spoke>"]
updated: <YYYY-MM-DD>
parent_folder: _projects
---

# <spoke> — Decision Log

_Auto-generated by `librarian plan-decision-log`. Do not hand-edit._

<!--
SHAPE CONTRACT — R-BIND-6 / R-BIND-3 (the binder decision-log contract).
Maintainer: librarian (skills/librarian/capabilities/plan-decision-log.sh).

This template is the instantiation surface for the generated decision surface; the
live file is re-derived from every plan manifest's decision_records[] on each run —
an append-immutable cross-plan ADR roll-up. Structure below MUST match the generator
emission:

  - Append-immutable intro prose (one row per declared decision_records[] entry across
    every <spoke>-spoke plan, grouped by parent_plan lineage; ADR bodies, rationale,
    and option-tables STAY at the linked path; superseded records are forward-linked,
    never deleted).
  - One `## Lineage: <lineage>` section per lineage group.
  - Columns in order: | ADR | Title | Status | Path | Superseded-by | Created | Plan-origin |.
      ADR           — the ADR-NN id.
      Title         — the decision title.
      Status        — proposed | accepted | rejected | deprecated | superseded.
      Path          — relative-path markdown link to the ADR body (body stays there).
      Superseded-by — the superseding ADR id (with a ✓ when that ADR is in-projection);
                      — when not superseded.
      Created       — the ISO creation date, or —.
      Plan-origin   — the contributing plan slug.
  - Empty spoke renders a valid empty decision log.
-->

Append-immutable cross-plan ADR roll-up (R-BIND-6): one row per declared
`decision_records[]` entry across every `<spoke>`-spoke plan, grouped by `parent_plan`
lineage. ADR bodies, rationale, and option-tables stay at the linked path. Superseded
records are forward-linked (Superseded-by), never deleted from the projection.

## Lineage: <lineage>

| ADR | Title | Status | Path | Superseded-by | Created | Plan-origin |
|---|---|---|---|---|---|---|
| <ADR-NN> | <title> | <status> | [<path>](<path>) | <ADR-NN or —> | <YYYY-MM-DD or —> | <plan> |
